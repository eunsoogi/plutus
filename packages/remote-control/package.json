{
  "name": "@plutus/remote-control",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. packages/remote-control/src/remote-control.test.ts",
    "test:unit": "vitest run --root ../.. packages/remote-control/src/remote-control.test.ts",
    "test:integration": "vitest run --root ../.. packages/remote-control/src/remote-control.test.ts"
  },
  "dependencies": {
    "zod": "^3.25.28"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
