import { fixtureIds, fixtureNow } from "./ids";

export const mvpProfile = {
  id: fixtureIds.profile,
  displayName: "MVP Investor",
  createdAt: fixtureNow,
  updatedAt: fixtureNow,
  deletedAt: null,
} as const;
