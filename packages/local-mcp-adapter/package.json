{
  "name": "@plutus/local-mcp-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "cd ../.. && vitest run packages/local-mcp-adapter/src",
    "test:unit": "cd ../.. && vitest run packages/local-mcp-adapter/src",
    "test:integration": "cd ../.. && vitest run packages/local-mcp-adapter/src"
  },
  "dependencies": {
    "@plutus/domain": "workspace:*",
    "@plutus/local-tools": "workspace:*",
    "zod": "^3.25.28"
  }
}
